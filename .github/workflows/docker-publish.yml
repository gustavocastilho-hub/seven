name: Build and Push Image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/strategicai-hub/seven:latest
            ghcr.io/strategicai-hub/seven:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trigger Portainer redeploy (all services)
        if: success()
        env:
          PORTAINER_URL: ${{ secrets.PORTAINER_URL }}
          PORTAINER_TOKEN: ${{ secrets.PORTAINER_TOKEN }}
          SERVICES: seven_seven-api seven_seven-worker seven_seven-scheduler
          # Digest imutável da imagem recém-buildada. Forçamos ele no spec
          # para garantir que Swarm puxe EXATAMENTE esta versão (sem depender
          # do cache local de :latest que pode ter sha256 antigo pinado).
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
          IMAGE_REPO: ghcr.io/strategicai-hub/seven
        run: |
          if [ -z "$PORTAINER_URL" ] || [ -z "$PORTAINER_TOKEN" ]; then
            echo "PORTAINER_URL or PORTAINER_TOKEN not set, skipping redeploy."
            exit 0
          fi
          if [ -n "$IMAGE_DIGEST" ]; then
            IMAGE_REF="${IMAGE_REPO}:latest@${IMAGE_DIGEST}"
          else
            IMAGE_REF="${IMAGE_REPO}:latest"
          fi
          echo "Deploying image: $IMAGE_REF"
          for SVC_NAME in $SERVICES; do
            echo "Updating $SVC_NAME..."
            SVC=$(curl -fsSLk -H "X-API-Key: $PORTAINER_TOKEN" \
              "$PORTAINER_URL/api/endpoints/1/docker/services" \
              | jq -r ".[] | select(.Spec.Name==\"$SVC_NAME\")")
            SVC_ID=$(echo "$SVC" | jq -r '.ID')
            VER=$(echo "$SVC" | jq -r '.Version.Index')
            SPEC=$(echo "$SVC" | jq \
              --arg img "$IMAGE_REF" \
              '.Spec
                | .TaskTemplate.ForceUpdate += 1
                | .TaskTemplate.ContainerSpec.Image = $img')
            curl -fsSLk -X POST \
              -H "X-API-Key: $PORTAINER_TOKEN" \
              -H "Content-Type: application/json" \
              -d "$SPEC" \
              "$PORTAINER_URL/api/endpoints/1/docker/services/$SVC_ID/update?version=$VER"
            echo " -> $SVC_NAME updated"
          done
